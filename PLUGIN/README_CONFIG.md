## 🎨 Web 通道专用配置

为了优化 Web 通道的用户体验，插件支持以下专用配置：

### 配置示例
```json5
{
  plugins: {
    entries: {
      "qr-code": {
        enabled: true,
        config: {
          // Python 配置
          pythonPath: "python3",
          enableAdvancedFeatures: true,
          
          // Web 通道专用配置
          webChannel: {
            enabled: true,
            outputDirectory: "/Users/yourname/Pictures/qr-codes",
            assetsDirectory: "/Users/yourname/Pictures/qr-assets", 
            autoProvideBase64: false,
            maxFileSize: 5242880  // 5MB
          }
        }
      }
    }
  }
}
```

### 配置选项说明

#### `webChannel.enabled` (boolean, default: false)
- 启用 Web 通道专用功能
- 当检测到 webchat 通道时自动激活特殊处理逻辑

#### `webChannel.outputDirectory` (string, default: "~/Pictures/qr-codes")
- 指定生成的 QR 码图片保存目录
- 插件会自动创建目录（如果不存在）
- 支持相对路径和绝对路径
- `~` 会被自动展开为用户主目录

#### `webChannel.assetsDirectory` (string, default: "~/Pictures/qr-assets")  
- 指定素材文件（logo、模板等）目录
- 用户可以在此目录放置 logo 图片供美化功能使用
- 在生成命令中可以直接引用此目录中的文件

#### `webChannel.autoProvideBase64` (boolean, default: false)
- 自动在响应中包含 Base64 编码的图片数据
- 如果设为 `true`，会同时提供文件路径和 Base64 数据
- 如果设为 `false`，会询问用户是否需要 Base64 数据

#### `webChannel.maxFileSize` (number, default: 5242880 = 5MB)
- 限制生成文件的最大大小（字节）
- 防止生成过大的图片文件

### 使用示例

#### 1. 基本使用（默认配置）
```bash
# 生成二维码，文件保存到 ~/Pictures/qr-codes/
生成一个二维码，内容是 https://example.com
```

#### 2. 使用自定义素材
```bash
# 假设你有 ~/Pictures/qr-assets/company-logo.png
生成一个带 logo 的二维码，内容是 https://company.com，logo 是 company-logo.png
```

#### 3. 获取 Base64 数据
```bash
# 插件会询问是否需要 Base64 数据
生成一个红色二维码，内容是 https://example.com

# 或者直接请求 Base64
生成一个二维码并提供 base64 数据，内容是 https://example.com
```

### 目录结构建议

```
~/Pictures/
├── qr-codes/           # 自动生成的 QR 码
│   ├── qr_20260205_114500.png
│   ├── qr_20260205_114630.png
│   └── ...
└── qr-assets/          # 用户提供的素材
    ├── company-logo.png
    ├── brand-colors.json  
    └── templates/
        └── event-banner.png
```

### 权限说明

- 插件需要写入指定目录的权限
- 首次使用时会自动创建目录
- 如果目录不可写，会回退到系统临时目录

### 故障排除

#### 目录创建失败
- 检查指定路径是否有写入权限
- 确保父目录存在
- 尝试使用绝对路径而非相对路径

#### Base64 生成失败  
- 检查生成的文件是否存在
- 确保文件大小不超过 `maxFileSize` 限制
- 验证文件格式是否支持（PNG/JPG/SVG）

#### 素材文件找不到
- 确认素材文件在 `assetsDirectory` 中
- 使用文件名而非完整路径（插件会自动查找）
- 检查文件扩展名是否正确