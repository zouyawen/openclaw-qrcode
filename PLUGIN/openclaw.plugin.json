{
  "id": "openclaw-qr-code",
  "name": "QR Code Plugin",
  "description": "Handle all QR code operations with advanced features including generation, decoding, beautification, and smart auto-detection of user intent.",
  "companionSkill": "qr-code",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "autoHandleAllQRRequests": {
        "type": "boolean", 
        "default": true,
        "description": "Automatically handle all QR code related requests"
      },
      "pythonPath": {
        "type": "string",
        "default": "python3",
        "description": "Path to Python executable for advanced QR operations"
      },
      "enableAdvancedFeatures": {
        "type": "boolean",
        "default": true,
        "description": "Enable advanced features requiring Python (logo embedding, gradient colors, etc.)"
      },
      "outputDirectory": {
        "type": "string",
        "default": "./qr-codes/",
        "description": "Directory for storing generated QR images when using web channel"
      },
      "assetsDirectory": {
        "type": "string", 
        "default": "./qr-assets/",
        "description": "Directory for storing logo and asset files when using web channel"
      },
      "autoProvideBase64": {
        "type": "boolean",
        "default": true,
        "description": "Automatically provide Base64 encoded image for web channel users"
      },
      "defaultColor": {
        "type": "string",
        "default": "#1976D2",
        "description": "Default QR code color when not specified"
      }
    }
  },
  "uiHints": {
    "autoHandleAllQRRequests": {
      "label": "Auto Handle All QR Requests",
      "help": "Enable automatic detection and handling of all QR code related conversations"
    },
    "pythonPath": {
      "label": "Python Path",
      "placeholder": "python3",
      "help": "Path to Python executable (default: python3)"
    },
    "enableAdvancedFeatures": {
      "label": "Enable Advanced Features",
      "help": "Requires Python with qrcode[pil], pillow, and pyzbar packages installed"
    },
    "outputDirectory": {
      "label": "Output Directory",
      "placeholder": "./qr-codes/",
      "help": "Where to save generated QR images. Use ~/ for home directory, ./ for workspace relative."
    },
    "assetsDirectory": {
      "label": "Assets Directory", 
      "placeholder": "./qr-assets/",
      "help": "Where to store logo and asset files. Use ~/ for home directory, ./ for workspace relative."
    },
    "autoProvideBase64": {
      "label": "Auto Provide Base64",
      "help": "Automatically include Base64 encoded image in web responses"
    },
    "defaultColor": {
      "label": "Default Color",
      "placeholder": "#1976D2",
      "help": "Default QR code color (CSS color name or hex code)"
    }
  }
}